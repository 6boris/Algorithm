#include <algorithm>
#include <cmath>
#include <deque>
#include <iostream>
#include <iterator>
#include <malloc.h>
#include <map>
#include <math.h>
#include <queue>
#include <set>
#include <sstream>
#include <stack>
#include <string.h>
#include <string>
#include <utility>
#include <vector>
#define TIME std::ios::sync_with_stdio(false)
#define LL long long
#define MAX 233
#define INF 0x3f3f3f3f

using namespace std;
int main()
{
    queue<int> list;
    queue<int> que;
    // if (pTreeRoot == NULL) {
    //     return list;
    // }
    que.push(1);
    que.push(2);
    que.push(3);
    que.push(4);

    cout << que.size() << endl;

    while (!que.empty()) {
        cout << que.front() << " ";
        que.pop();
    }
    while () {

    }
    for(int i=0 ; i<10 ; i++){
        
    }

    return 0;
}